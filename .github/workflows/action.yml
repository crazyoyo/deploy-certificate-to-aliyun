name: Auto Renew and Deploy SSL Certificates

on:
  push:
    branches:
      - main
  schedule:
    # 每月 1 日执行（间隔 28~31 天）。
    #
    # 间隔必须小于 60 天：GitHub 对 public 仓库「60 天无活动」会自动禁用
    # 定时任务，而原 '0 0 20 */2 *' 的实际间隔是 59~62 天，六个间隔里五个
    # 超线，导致每隔两个月就静默失效一次（2026-05-20 那次即因此丢失，
    # 证书在 06-18 过期无人察觉）。
    #
    # 又不宜更密：runner 是一次性的，~/.acme.sh 状态不落盘，所以每次运行
    # 都是全新 --issue，acme.sh 无法按 ARI 窗口跳过，每跑一次就真签发一张
    # 新证书并在阿里云证书库新增一条记录。每月一次 = 12 张/年，证书有效期
    # 90 天，单次失败仍有约 59 天缓冲。
    #
    # 注意 upload_certs_to_aliyun.py 的 CertName 精度到天，同一天重复运行
    # 会撞 CertNameAlreadyExists。
    - cron: '0 0 1 * *'
  workflow_dispatch: # 允许在 Actions 页面手动触发

jobs:
  renew-deploy-cert:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8'

    - name: Install acme.sh
      env:
        EMAIL: ${{ secrets.EMAIL}}
      run: |
        sudo apt-get update
        sudo apt-get install -y socat
        curl https://get.acme.sh | sh -s email="${EMAIL}"

    - name: Prepare acme.sh credentials
      run: |
        mkdir -p ~/.acme.sh
        IFS=',' read -r -a domain_array <<< "${{ secrets.DOMAINS }}"
        for domain in "${domain_array[@]}"; do
          mkdir -p ~/certs/${domain}
        done

    - name: Obtain SSL Certificates
      env:
        DOMAINS: ${{ secrets.DOMAINS }}
        Ali_Key: ${{ secrets.ALIYUN_ACCESS_KEY_ID }}
        Ali_Secret: ${{ secrets.ALIYUN_ACCESS_KEY_SECRET }}
      run: |
        IFS=',' read -r -a domain_array <<< "${DOMAINS}"
        for domain in "${domain_array[@]}"; do
          ~/.acme.sh/acme.sh --issue --dns dns_ali -d "*.${domain}" \
          --key-file ~/certs/${domain}/privkey.pem --fullchain-file ~/certs/${domain}/fullchain.pem
        done


    - name: Install Python dependencies
      run: pip install -r requirements.txt

    - name: Upload certificates to Alibaba Cloud CDN
      env:
        ALIYUN_ACCESS_KEY_ID: ${{ secrets.ALIYUN_ACCESS_KEY_ID }}
        ALIYUN_ACCESS_KEY_SECRET: ${{ secrets.ALIYUN_ACCESS_KEY_SECRET }}
        # 要设置域名的二级域名，例如要设置*.example.com，这里填写的就是example.com, 多个域名用英文逗号隔开
        DOMAINS: ${{ secrets.DOMAINS }}
        # 设置阿里云cdn域名，一般是三级域名，例如cdn.example.com，需要跟上面的DOMAINS对应，否则会设置错误
        ALIYUN_CDN_DOMAINS: ${{ secrets.ALIYUN_CDN_DOMAINS }}
  
      run: python upload_certs_to_aliyun.py

    - name: Clean up
      env:
        DOMAINS: ${{ secrets.DOMAINS }}
      run: |
        IFS=',' read -r -a domain_array <<< "${DOMAINS}"
        for domain in "${domain_array[@]}"; do
          rm -rf ~/certs/${domain}
        done